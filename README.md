# Armory

A robotics inference engine for serving an action-chunking policy to multiple robots concurrently.

## Setup

For local server development, local LIBERO evaluation, or editing a third-party
dependency, initialize the submodules and install the project environment:

```bash
git submodule update --init --recursive
GIT_LFS_SKIP_SMUDGE=1 uv sync
```

## Repository Structure
TODO: manual
document repo structure and packaging, for lightweight client + local development

## Running commands

Everything can and should be run on [Modal](modal.com). The free tier offers $30/month of credits, which is more than enough for getting started with this repository. For instructions, refer to the [Modal guide](scripts/modal/README.md).

## Acknowledgmenets
TODO: manual
acknowledge that much of codebase was evolved from openpi
